using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using YG;

public class isDesktopOrMobile : MonoBehaviour
{
    enum ChoseType { Desktop = 0, Mobile = 1 }

    [SerializeField] private ChoseType type;

    void Start()
    {
        //if()
    }
}
